let moment = require('moment');
let async = require('async');

module.exports = function (app, express) {
    var Router = express.Router();

    Router.post('/rateSeller', function (req, res) {
        
    });
    return Router();
}